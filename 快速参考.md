# ƒlaunch Protocol 快速参考指南

## 📋 核心合约速查表

### 主要合约

| 合约名称 | 文件路径 | 核心功能 | 关键方法 |
|---------|---------|---------|---------|
| **Flaunch** | `src/contracts/Flaunch.sol` | 代币创建工厂 | `flaunch()`, `initializeBridge()` |
| **PositionManager** | `src/contracts/PositionManager.sol` | 核心协调器（Hook） | `flaunch()`, `swap()` |
| **Memecoin** | `src/contracts/Memecoin.sol` | ERC20 代币实现 | `mint()`, `burn()`, `creator()` |
| **FairLaunch** | `src/contracts/hooks/FairLaunch.sol` | 公平启动机制 | `createPosition()`, `endFairLaunch()` |
| **BidWall** | `src/contracts/bidwall/BidWall.sol` | 渐进式买单墙 | `deposit()`, `reposition()` |
| **FeeDistributor** | `src/contracts/hooks/FeeDistributor.sol` | 手续费分配 | `_distributeFees()` |
| **InternalSwapPool** | `src/contracts/hooks/InternalSwapPool.sol` | 内部交换池 | `_internalSwap()` |
| **MemecoinTreasury** | `src/contracts/treasury/MemecoinTreasury.sol` | 资金库管理 | `executeAction()` |

---

## 🔄 关键流程速查

### 1. 代币启动流程
```
PositionManager.flaunch()
  → Flaunch.flaunch()
    → 部署 Memecoin
    → 部署 MemecoinTreasury
    → 铸造 ERC721 NFT
    → 发送初始供应量到 PositionManager
```

### 2. 交易流程
```
用户调用 swap()
  → PositionManager.beforeSwap()
    → InternalSwapPool 检查可用代币
    → 如果有，直接交换
  → Uniswap 执行交换
  → PositionManager.afterSwap()
    → 捕获手续费
    → FeeDistributor 分配
    → BidWall 更新（如果启用）
```

### 3. 手续费分配顺序
```
总手续费
  ↓
推荐人费用 (referrerFee)
  ↓
协议费用 (protocolFee)
  ↓
创建者费用 (creatorFee)
  ↓
BidWall 费用 (剩余部分)
```

---

## 📊 数据结构速查

### FlaunchParams
```solidity
struct FlaunchParams {
    string name;                    // 代币名称
    string symbol;                  // 代币符号
    string tokenUri;                // NFT URI
    uint initialTokenFairLaunch;    // 公平启动代币数量
    uint fairLaunchDuration;        // 公平启动持续时间
    uint premineAmount;             // 预挖数量
    address creator;                // 创建者地址
    uint24 creatorFeeAllocation;    // 创建者费用分配（基点）
    uint flaunchAt;                 // 启动时间戳
    bytes initialPriceParams;       // 初始价格参数
    bytes feeCalculatorParams;      // 费用计算器参数
}
```

### FairLaunchInfo
```solidity
struct FairLaunchInfo {
    uint startsAt;      // 开始时间
    uint endsAt;        // 结束时间
    int24 initialTick; // 初始 tick
    uint revenue;       // 收入
    uint supply;        // 供应量
    bool closed;       // 是否已关闭
}
```

### PoolInfo (BidWall)
```solidity
struct PoolInfo {
    bool disabled;           // 是否禁用
    bool initialized;       // 是否已初始化
    int24 tickLower;        // 下限 tick
    int24 tickUpper;        // 上限 tick
    uint pendingETHFees;    // 待处理的 ETH 费用
    uint cumulativeSwapFees; // 累计交换费用
}
```

---

## 🎯 Hook 点速查

### PositionManager Hook 权限
```solidity
beforeInitialize: true      // 防止外部初始化
beforeSwap: true           // InternalSwapPool
afterSwap: true            // FeeDistributor, BidWall
beforeSwapReturnDelta: true // InternalSwapPool
afterSwapReturnDelta: true  // FeeDistributor
```

### Hook 执行顺序
```
1. beforeSwap (PositionManager)
   └─ InternalSwapPool._internalSwap()
   
2. Uniswap 执行交换

3. afterSwap (PositionManager)
   ├─ FeeDistributor._distributeFees()
   └─ BidWall.deposit() (如果启用)
```

---

## 💰 费用相关常量

### 费用计算
- **基点 (Basis Points)**: 1 基点 = 0.01%
- **MAX_CREATOR_ALLOCATION**: 10000 (100%)
- **BASE_SWAP_FEE** (FlayHooks): 100 (1%)

### 阈值
- **MIN_DISTRIBUTE_THRESHOLD**: 0.001 ether
- **BidWall swapFeeThreshold**: 0.1 ether (可配置)

---

## 🔐 权限角色

### ProtocolRoles
- `DEFAULT_ADMIN_ROLE`: 默认管理员
- `PROTOCOL_ROLE`: 协议角色
- `FLAY_GOVERNANCE_ROLE`: FLAY 治理角色

### 关键权限检查
- `onlyPositionManager`: 只有 PositionManager 可以调用
- `onlyFlaunch`: 只有 Flaunch 合约可以调用
- `onlyCreator`: 只有创建者可以调用
- `onlyOwner`: 只有所有者可以调用

---

## 🛠️ 常用工具函数

### CurrencySettler
- `settle()`: 结算货币余额
- `take()`: 从池子中提取货币

### MemecoinFinder
- `findMemecoin()`: 从 PoolKey 中查找 memecoin 地址

### TickFinder
- `findTickBelow()`: 查找指定价格下方的 tick
- `findTickAbove()`: 查找指定价格上方的 tick

### TokenSupply
- `INITIAL_SUPPLY`: 初始供应量常量

---

## 📝 事件速查

### 核心事件
```solidity
// Flaunch
event TokenBridging(uint tokenId, uint chainId, address memecoin);
event TokenBridged(uint tokenId, uint chainId, address memecoin, uint messageSource);

// PositionManager
event PoolCreated(PoolId poolId, address memecoin, ...);
event PoolScheduled(PoolId poolId, uint flaunchesAt);
event PoolSwap(...);
event FairLaunchBurn(PoolId poolId, uint unsoldSupply);

// FairLaunch
event FairLaunchCreated(PoolId poolId, uint tokens, uint startsAt, uint endsAt);
event FairLaunchEnded(PoolId poolId, uint revenue, uint supply, uint endedAt);

// BidWall
event BidWallInitialized(PoolId poolId, uint eth, int24 tickLower, int24 tickUpper);
event BidWallDeposit(PoolId poolId, uint added, uint pending);
event BidWallRepositioned(PoolId poolId, uint eth, int24 tickLower, int24 tickUpper);
event BidWallClosed(PoolId poolId, address recipient, uint eth);
```

---

## 🧪 测试文件速查

### 主要测试文件
- `test/Flaunch.t.sol`: Flaunch 合约测试
- `test/PositionManager.t.sol`: PositionManager 测试
- `test/creators/*.sol`: 创建者相关测试
- `test/bidwall/*.sol`: BidWall 测试
- `test/treasury/*.sol`: 资金库测试
- `test/fees/*.sol`: 费用相关测试

### 运行测试
```bash
# 运行所有测试
forge test

# 运行特定测试文件
forge test --match-path test/Flaunch.t.sol

# 详细输出
forge test -vvv

# 调试模式
forge test --debug <test_name>
```

---

## 🔍 调试技巧

### 1. 使用 console.log
```solidity
import {console} from "forge-std/console.sol";

function testSomething() {
    console.log("Value:", value);
}
```

### 2. 追踪事件
```bash
# 查看事件日志
forge test -vvv | grep "event"
```

### 3. Gas 分析
```bash
forge test --gas-report
```

---

## 📚 接口速查

### 核心接口
- `IFlaunch`: Flaunch 合约接口
- `IMemecoin`: Memecoin 合约接口
- `IPositionManager`: PositionManager 接口
- `IFeeCalculator`: 费用计算器接口
- `ITreasuryManager`: 资金库管理器接口
- `ITreasuryAction`: 资金库操作接口

### 查看接口定义
```bash
# 查看合约 ABI
forge inspect <ContractName> abi

# 查看合约存储布局
forge inspect <ContractName> storage
```

---

## 🚀 部署地址速查

### Base 主网
- **Flaunch**: `0x6A53F8b799bE11a2A3264eF0bfF183dCB12d9571`
- **PositionManager**: `0x51Bba15255406Cfe7099a42183302640ba7dAFDC`
- **BidWall**: `0x66681f10BA90496241A25e33380004f30Dfd8aa8`
- **FairLaunch**: `0xCc7A4A00072ccbeEEbd999edc812C0ce498Fb63B`

### Base Sepolia 测试网
- **Flaunch**: `0x7D375C9133721083DF7b7e5Cb0Ed8Fc78862dfe3`
- **PositionManager**: `0x9A7059cA00dA92843906Cb4bCa1D005cE848AFdC`

---

## ⚠️ 常见错误

### 错误代码速查
```solidity
// Flaunch
error CallerIsNotPositionManager();
error CreatorFeeAllocationInvalid(uint24 allocation, uint maxAllocation);
error InvalidFlaunchSchedule();
error PremineExceedsInitialAmount(uint buyAmount, uint initialSupply);

// PositionManager
error InsufficientFlaunchFee(uint paid, uint required);
error TokenNotFlaunched(uint flaunchesAt);
error UnknownPool(PoolId poolId);

// FairLaunch
error CannotModifyLiquidityDuringFairLaunch();
error CannotSellTokenDuringFairLaunch();

// BidWall
error CallerIsNotCreator();
error NotPositionManager();
```

---

## 🔗 相关资源

### 文档
- [Uniswap V4 文档](https://docs.uniswap.org/sdk/v4/overview)
- [Foundry 文档](https://book.getfoundry.sh/)
- [项目 README](./README.md)

### 工具
- [Etherscan (Base)](https://basescan.org/)
- [Foundry Book](https://book.getfoundry.sh/)

---

## 📌 重要提示

1. **Gas 优化**: Internal Swap Pool 可以减少 Gas 成本
2. **价格保护**: BidWall 提供自动价格支撑
3. **公平性**: Fair Launch 确保所有参与者平等
4. **长期激励**: 开发者收入分成对齐长期利益

---

**最后更新**: 请参考最新代码和文档获取最新信息。

